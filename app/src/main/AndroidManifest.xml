<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uts_h1101171023">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sintaxmanifest"></activity>
        <activity android:name=".sintaxmateri" />
        <activity android:name=".sintaxsiaran" />
        <activity android:name=".sintaxlayanan" />
        <activity android:name=".sintaxactiviti" />
        <activity android:name=".Komponen5" />
        <activity android:name=".komponen4" />
        <activity android:name=".Komponen3" />
        <activity android:name=".Komponen2" />
        <activity android:name=".Komponen1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>